# Process CLI — 设计评估 & 开发路线图

> **最后更新**: 2026-02-09
> **当前版本**: 0.1.0 (Alpha)
> **架构愿景**: [DESIGN.md](DESIGN.md)

---

## 零、项目定位

### 核心理念

> **在 AI 能瞬间生成一切的时代，人很容易变成橡皮图章——AI 给什么就接什么，失去了自己的判断。**
> **Process CLI 不是帮你更快写代码的工具，而是帮你成为更好的决策者。**
> **过程本身就有意义。思考本身是愉悦的。**

### 长期定位

Process CLI 不是一个开发流程工具，而是一个**通用决策流水线引擎**。

- 核心架构：**Pass Engine**（一切皆 Pass，Phase 是 Pass 的预设组合）
- 软件开发是第一个"领域包"，同样的模型可应用于任何创造性工作
- 陪伴项目的**整个生命周期**（诞生→成长→成熟→演化→退役）
- 决策周期是一等公民，一个项目可以经历多轮决策周期

详见 [DESIGN.md](DESIGN.md)。

### 设计哲学

| 表面上看起来像 | 实际上是 |
|----------------|----------|
| Diverge/Converge 是给简单事加复杂度 | **强制看到至少两个方案再选**，对抗"第一个看起来不错就接受"的惰性 |
| 前四阶段一行代码没写 | **在写代码之前把决策做扎实**，写的时候才不会反复推倒 |
| Phase 只进不退 | **承诺机制**。做了决定就要承担后果，不能随便推翻自己 |
| REJECTED_APPROACHES 没人看 | 人**写下来的时候**就在内化判断，记录本身就是思考过程 |
| Branch Review 的四角色太重 | 让人看到**同一件事的四种解读**，然后自己裁决，这就是决断力 |
| Postmortem 走过场 | 对自己过去决策的反思，才是决断力**真正成长**的时刻 |

### 一句话总结

**Process CLI = 通用决策流水线引擎。引擎不可插拔，引擎上跑的所有东西都可插拔。**

---

## 一、设计评估

### ✅ 设计亮点

| 方面 | 评价 | 详述 |
|------|------|------|
| **Trait 抽象** | ⭐⭐⭐⭐⭐ | `AiProvider`, `Generator`, `Check`, `ReviewTemplate` 四个核心 trait 定义清晰，扩展点合理。新增 Provider/Check 只需 impl trait + register，零侵入核心代码 |
| **Workspace 拆分** | ⭐⭐⭐⭐ | 6 个 crate 职责分明：core(状态) / ai(智能) / config(配置) / generators / checks / reviews，编译粒度好 |
| **配置分层** | ⭐⭐⭐⭐⭐ | 默认值 → 全局 `~/.config/` → 项目 `.process/` → 环境变量，优先级清晰，符合 12-Factor 理念 |
| **Phase 状态机** | ⭐⭐⭐⭐ | `PartialOrd` 派生 + `set_phase` 只进不退 + `check_phase` 门控，简洁且正确 |
| **AI Pipeline 模式** | ⭐⭐⭐⭐ | 所有 AI 命令遵循统一流程：检查状态 → 读输入 → 构建 Prompt → 调用 AI → 清洗输出 → 保存 → 推进状态 |
| **文档完备度** | ⭐⭐⭐⭐ | ARCHITECTURE.md / COMMANDS.md / CONFIGURATION.md / EXTENDING.md 四篇文档覆盖全面 |

### ⚠️ 设计层面的问题

#### 1. 🔴 缺少「适配已有项目」的能力（你提到的问题）

**这是最大的架构缺口。** 当前整个工具只能从零开始（`init` → `seed` → ...），完全没有考虑这种场景：

> "我已经有一个正在开发的项目，想用 process-cli 来管理后续的迭代流程。"

**缺失的能力：**
- 没有 `import` / `adopt` 命令 — 无法从已有代码库推断出 seed/rules
- 没有项目扫描能力 — 无法检测已有项目的语言、框架、目录结构
- 没有从中间 Phase 开始的机制 — 如果项目已过了 Seed/Diverge 阶段，无法跳入 Branching 直接使用分支工作流
- Skeleton 只生成 YAML 描述，没有 `skeleton-apply` 来实际创建文件

**这意味着整个工具只能用于全新项目，严重限制了适用范围。**

#### 2. 🟡 Skeleton 是「规划」而非「执行」

当前 `skeleton` 命令：
```
seed + rules → AI → skeleton.yaml (描述文件)  ← 到此结束
```

缺少关键的下一步：
```
skeleton.yaml → 实际创建目录和文件 → git commit → git tag skeleton-v1
```

Bash 版本也有这个问题——Skeleton 只是个蓝图，从来不会被应用到文件系统。

#### 3. 🟡 Prompt 硬编码，不可定制

所有 AI Prompt 以 Rust `format!()` 字面量写在各命令文件里：
- `diverge.rs` 里硬编码了提案生成的 Prompt
- `converge.rs` 里硬编码了规则提取的 Prompt
- `skeleton.rs` 里硬编码了骨架生成的 Prompt

问题：
- 用户无法调整 Prompt（如切换语言、添加领域约束）
- 不同项目类型（前端/后端/嵌入式）需要不同的 Prompt 策略
- `tera` 模板引擎已声明为依赖但从未使用

#### 4. 🟡 命令实现不遵循设计文档的 Command Trait

设计文档定义了一个优雅的 `Command` trait：
```rust
pub trait Command: Send + Sync {
    fn name(&self) -> &'static str;
    fn required_phase(&self) -> Option<PhaseRequirement>;
    async fn execute(&self, ctx: &AppContext, args: &CommandArgs) -> Result<()>;
}
```

但实际实现是**散装函数**——每个命令都是独立的 `pub async fn execute(...)` 加上 `main.rs` 里的手动 match dispatch。这导致：
- 无法实现插件化命令注册
- 每个命令重复同样的状态检查/配置加载代码
- 无法统一处理 `--verbose`/`--quiet` 等全局参数

#### 5. 🟡 init 命令生成的 seed.yaml 模板与 PROCESS.md 不一致

`PROCESS.md` 定义了 6 个必填字段：
```yaml
idea, target_user, constraints, non_goals, success_criteria, reversibility_budget
```

但 Rust `init.rs` 生成的模板只有 3 个字段：
```yaml
project_name, description, goals
```

同时没有 `seed-validate` 命令来验证这些字段。

#### 6. 🟢 错误处理策略不一致

- `ProcessState::load()` 在 `.state.yaml` 不存在时静默返回默认值（没有 `.process/` 目录也不报错）
- `skeleton-validate` 不推进状态，但 `diverge-validate` 和 `converge-validate` 会推进
- `ai-config test` 手动构建 Registry 而不复用 `utils::create_ai_registry()`

### 🔑 从「增强决断力」视角发现的设计缺口

#### 7. 🔴 决策点不够显式

当前流程中，人的决策嵌在 AI 输出里——AI 生成提案，人看完跑 `converge`，但**人到底做了什么决定、基于什么理由**，没有被显式记录。

`decisions_log.yaml` 虽然在 `init` 时创建了，但**没有任何命令往里写**。

每个 Phase 转换时应自动要求人填写决策理由：

```yaml
# .process/decisions_log.yaml
- phase_transition: "diverge → converge"
  decision: "选择了方案 A 的整体架构 + 方案 B 的存储层"
  reasoning: "方案 A 更简单但存储层设计粗糙，B 的存储层考虑了并发"
  confidence: "high"  # high / medium / low
  revisit_trigger: "如果 QPS 超过 10k 需要重新评估"
  decided_by: "human"
  timestamp: "2026-02-06T14:30:00Z"
```

**不记录决策理由的流程只是仪式，记录了才是真正的判断力训练。**

#### 8. 🔴 缺少「人挑战 AI」的机制

现在的流程是：AI 生成 → 人验证格式 → 下一步。人太容易变成"看看 YAML 格式对不对"的验证者。

应在 validate 之前加一个 `challenge` 步骤，迫使人主动找 AI 方案的漏洞，而不是被动接受：

```bash
process diverge-challenge    # 人必须写出对每个方案的质疑
```

```yaml
# .process/diverge_challenges.yaml — 人必须填写，不能为空
challenges:
  - proposal: "方案 A"
    weaknesses_i_see: "没考虑离线场景"
    what_could_go_wrong: "网络断开时全部不可用"
    question_for_ai: "如果加离线支持，架构需要怎么改？"
  - proposal: "方案 B"
    weaknesses_i_see: "微服务运维成本太高"
    what_could_go_wrong: "团队只有 2 人，服务间调试会很痛苦"
    question_for_ai: "如何在不拆服务的情况下保持模块边界清晰？"
```

#### 9. 🟡 Review 冲突裁决过于粗糙

Branch Review 的多角色审查是最能锻炼决断力的环节，但当前冲突解决只有一行：
```yaml
conflict_resolution:
  policy: "human_final_say"
```

应要求人写下**为什么选了这一方而不是那一方**：

```yaml
# .process/branches/<name>-review.yaml 中新增
conflicts:
  - between: ["安全审计员", "性能工程师"]
    issue: "加密所有字段 vs 只加密敏感字段"
    human_ruling: "只加密敏感字段"
    reasoning: "P99 延迟约束 < 200ms，全字段加密会超标"
    risk_accepted: "非敏感字段如果被定义错误会导致泄露"
```

#### 10. 🟡 Postmortem 缺少「决策质量回顾」

Postmortem 不应只是 AI 生成的流程总结，更应是人回顾自己决策质量的反思环节：

```yaml
# .process/postmortem.yaml 中新增
decision_quality_review:
  - decision: "选择了方案 A"
    phase: 2
    outcome: "正确，架构到后期依然稳定"

  - decision: "INV-003: 所有响应 < 200ms"
    phase: 2
    outcome: "过于激进，后来放宽到 500ms"
    lesson: "设硬约束前应该先做基准测试"

  - decision: "跳过了方案 B 的事件驱动设计"
    phase: 2
    outcome: "后悔，Phase 4 的三个分支都在手动实现类似的事件机制"
```

**人对自己过去决策的反思，才是"决断力"真正成长的时刻。**

---

## 二、完整问题清单

### 🔴 Critical — 影响核心使用

| ID | 问题 | 类别 | 说明 |
|----|------|------|------|
| C-1 | 无法适配已有项目 | 架构缺陷 | 只能 init 全新项目，无法 import/adopt 已有代码库 |
| C-2 | Phase 4 (Branch Loop) 未实现 | 功能缺失 | 6 个分支命令全部缺失，这是日常开发使用最频繁的部分 |
| C-3 | Phase 5-6 未实现 | 功能缺失 | stabilize / postmortem / done 三个收尾命令缺失 |
| C-4 | seed.yaml 模板与规范不符 | Bug | init 生成的模板字段与 PROCESS.md 定义的不一致 |
| C-5 | 决策点无显式记录 | 核心缺陷 | Phase 转换时不要求人记录决策理由，decisions_log.yaml 形同虚设 |
| C-6 | 缺少 challenge 机制 | 核心缺陷 | 人只是验证 YAML 格式，没有被迫思考 AI 方案的漏洞 |

### 🟡 Major — 影响可用性

| ID | 问题 | 类别 | 说明 |
|----|------|------|------|
| M-1 | 测试覆盖极低 | 质量 | 全项目仅 5 个测试，核心 crate 零测试 |
| M-2 | Prompt 硬编码 | 可维护性 | 无法定制/切换语言，tera 依赖浪费 |
| M-3 | `seed-validate` 命令缺失 | 功能缺失 | Bash 版有，Rust 版遗漏 |
| M-4 | `learn` / `friction` 命令缺失 | 功能缺失 | Bash 版有，Rust 版遗漏 |
| M-5 | `status` 命令过于简陋 | 功能不足 | Bash 版有进度条、分支状态、文件存在性检查；Rust 版只显示 Phase |
| M-6 | Skeleton 无 apply 能力 | 功能缺失 | 只生成 YAML 蓝图，不创建实际文件 |
| M-7 | 未使用 Command Trait | 架构债务 | 实现与设计文档脱节，散装函数无法插件化 |
| M-8 | 三个 crate 完全空壳 | 功能缺失 | checks / generators / reviews 只有 `// placeholder` |
| M-9 | Review 冲突裁决过于粗糙 | 决断力 | 多角色冲突时只有 policy 字段，不要求人写裁决理由 |
| M-10 | Postmortem 缺少决策质量回顾 | 决断力 | 只有 AI 总结，没有人对自己过去决策的反思环节 |

### 🟢 Minor — 值得改进

| ID | 问题 | 类别 | 说明 |
|----|------|------|------|
| m-1 | `verbose` 标志从未使用 | 死代码 | cli.rs 定义了但无逻辑消费 |
| m-2 | `skeleton-validate` 不推进状态 | 不一致 | 其他 validate 命令会推进 |
| m-3 | AI registry 重复构建 | 代码重复 | ai_config.rs 手动构建而非复用 utils |
| m-4 | 状态文件不存在时静默回退 | 健壮性 | 在未 init 的目录运行不报错 |
| m-5 | 多个 workspace 依赖未使用 | 整洁度 | tera, include_dir, dialoguer, console, indicatif, uuid, thiserror, futures |
| m-6 | Claude 默认模型过时 | 配置 | 硬编码 `claude-3-5-sonnet-20241022`，应更新 |
| m-7 | AI Provider 只有 Claude | 可扩展性 | OpenAI/Ollama/Manual 仅存在于注释 |

---

## 三、开发路线图

### Milestone 0: 基础修复（预计 1-2 天）

> 先把地基打牢，修正已有实现中的 bug 和不一致。

- [ ] **修正 seed.yaml 模板** — 对齐 PROCESS.md 的 6 字段规范（idea, target_user, constraints, non_goals, success_criteria, reversibility_budget）
- [ ] **新增 `seed-validate` 命令** — 验证 seed.yaml 必填字段非空
- [ ] **统一 validate 行为** — 所有 validate 命令要么都推进状态、要么都不推进（建议：都不推进，只做验证）
- [ ] **修复状态文件加载** — 在 `.process/` 不存在时返回明确错误而非静默默认
- [ ] **复用 AI registry** — `ai-config test` 使用 `utils::create_ai_registry()`
- [ ] **清理未使用依赖** — 移除 workspace 中未使用的 crate 依赖
- [ ] **更新 Claude 默认模型** — 使用 `claude-sonnet-4-20250514`

### Milestone 1: 补齐 Bash 功能平替（预计 3-5 天）

> 达到与 legacy bash 脚本的功能对等。

- [ ] **`learn` 命令** — 向 `learnings.yaml` 追加记录
- [ ] **`friction` 命令** — 向 `friction.yaml` 追加记录
- [ ] **`branch new` 命令** — 创建分支假设 YAML
- [ ] **`branch start` 命令** — 验证假设、更新状态、创建 git 分支
- [ ] **`branch review` 命令** — 多角色 AI 审查
- [ ] **`branch abuse` 命令** — 恶意用户测试
- [ ] **`branch gate` 命令** — 合并门控检查（自动 + 交互确认）
- [ ] **`branch merge` 命令** — 标记分支为已合并
- [ ] **`stabilize` 命令** — 冻结不变量、检查未合并分支
- [ ] **`postmortem` 命令** — AI 生成回顾报告
- [ ] **`done` 命令** — 标记完成
- [ ] **增强 `status` 命令** — 进度条 + 分支状态 + 文件存在性检查

### Milestone 1.5: 决策深度增强（预计 2-3 天）⭐⭐ 核心差异化

> 这是让 Process CLI 从"流程管理工具"变成"决策训练框架"的关键里程碑。
> 没有这个里程碑，工具只是在管理 YAML 文件；有了它，工具在训练人的判断力。

#### 1.5a. 决策日志自动化

在每个 Phase 转换命令（`diverge`, `converge`, `skeleton`, `stabilize`）中，执行 AI 调用**之后、推进状态之前**，交互式要求人填写决策理由：

```
$ process converge
✔ AI 生成了规则...

━━━ 决策记录 (必填) ━━━
你做了什么决定？> 选择方案 A + B 的存储层
为什么？> A 更简单，但 B 的存储层考虑了并发
信心度 [high/medium/low]？> medium
什么情况下需要重新评估？> QPS 超过 10k
━━━━━━━━━━━━━━━━━━━━━━━

✔ 决策已记录到 decisions_log.yaml
✔ 状态更新为 Converge
```

- [ ] **决策记录交互流程** — 使用 `dialoguer` 实现交互式输入
- [ ] **`decisions_log.yaml` 追加写入** — 每条含 phase_transition, decision, reasoning, confidence, revisit_trigger, timestamp
- [ ] **`--skip-decision` 标志** — 允许跳过（但打印警告），不强制阻断

#### 1.5b. Challenge 命令

新增 `*-challenge` 系列命令（或作为 validate 的一部分），要求人在验证之前先写出质疑：

- [ ] **`diverge-challenge`** — 人必须写出对每个方案的质疑（weaknesses_i_see, what_could_go_wrong, question_for_ai）
- [ ] **`converge-challenge`** — 人必须写出对选中方案的担忧和对被拒方案的遗憾
- [ ] **Challenge 文件** — 保存到 `.process/diverge_challenges.yaml` 等
- [ ] **非空校验** — challenge 字段为空时拒绝继续

#### 1.5c. Review 冲突裁决增强

- [ ] **冲突检测** — 在 `branch review` 输出中识别角色间的分歧
- [ ] **裁决记录** — 要求人填写：human_ruling, reasoning, risk_accepted
- [ ] **写入 review YAML** — 冲突裁决记录到 `<branch>-review.yaml` 的 `conflicts` 字段

#### 1.5d. Postmortem 决策质量回顾

- [ ] **读取 decisions_log** — 列出所有历史决策
- [ ] **交互式评分** — 人逐条评估：outcome（正确/错误/待定）、lesson
- [ ] **写入 postmortem** — `decision_quality_review` 字段
- [ ] **AI 辅助反思** — AI 根据实际结果 + 人的评估生成改进建议

---

### Milestone 2: Adopt Pass 组（预计 3-4 天）⭐ 关键里程碑

> 解决最大的架构缺口——让工具可以在已有项目中使用。
> Adopt 是一组**独立的 analysis pass**，产出与正向流程兼容的 artifact。

#### 2a. adopt.scan-structure
- [ ] 扫描项目目录，检测语言/框架/目录结构
- [ ] 产出 `skeleton.yaml`（逆向工程）
- [ ] 可独立使用，不依赖其他 adopt pass

#### 2b. adopt.infer-conventions
- [ ] 从代码风格、命名模式推断项目规范
- [ ] 产出 `rules.yaml`（推断版）
- [ ] AI 辅助分析 + 人类确认

#### 2c. adopt.scan-git-history
- [ ] 从 git history、PR 记录中考古决策信息
- [ ] 产出 `decisions_log.yaml`（考古版）
- [ ] 即使不用后续流程，"决策考古"本身就有独立价值

#### 2d. adopt.scan-dependencies
- [ ] 分析项目依赖、技术栈
- [ ] 产出 `seed.yaml` 的部分字段

#### 2e. adopt.gap-analysis
- [ ] 识别缺失的决策记录
- [ ] 产出 `gap-report.yaml`

#### 2f. 入口命令
```bash
process adopt                    # 运行所有 adopt pass
process adopt scan-structure     # 只运行单个 pass
process adopt gap-analysis       # 只运行分析
```

### Milestone 3: Prompt 系统 ✅ DONE (2026-02-10)

> 每个 AI provider 一套独立的 prompt 模板，从硬编码迁移到 Tera 模板文件。

- [x] **启用 Tera 模板引擎** — 将 Prompt 从 Rust `format!()` 迁移到模板文件
- [x] **每个 provider 一套模板** — `prompts/claude/`、`prompts/_default/` (12 个模板)
- [x] **模板查找链** — 项目本地 provider → 项目本地 default → 内置 provider → 内置 default
- [x] **变量注入** — seed, rules, knowledge, branch info 作为模板变量
- [x] **内置模板嵌入** — 用 `include_dir!` 编译时嵌入默认模板

模板目录结构：
```
.process/prompts/
  claude/
    diverge.generate.md.tera
    diverge.challenge.md.tera
    converge.analyze.md.tera
    ...
  openai/
    ...
  _default/
    ...
```

### Milestone 4: 测试体系 ✅ DONE (2026-02-10)

> 建立信心，保障后续迭代安全。32 个测试全部通过。

- [x] **`process-core` 单元测试** (11) — Phase 排序、set_phase 只进不退、load/save 生命周期
- [x] **`process-config` 单元测试** (3) — 默认值正确性、无文件时使用默认
- [x] **`process-ai` 单元测试** (9) — MockProvider、registry auto-detect 优先级
- [x] **`process-cli` 单元测试** (9) — PromptEngine 渲染、strip_markdown
- [ ] **CI 配置** — GitHub Actions: `cargo test` + `cargo clippy` + `cargo fmt --check`

### Milestone 5: 更多 AI Provider ✅ DONE (2026-02-10)

- [x] **OpenAI Provider** — GPT-4o, `OPENAI_API_KEY`, priority 80
- [x] **Ollama Provider** — 本地 llama3.1, ping 检测, priority 30
- [x] **Claude CLI Provider** — 调用 `claude` 命令行, priority 95
- [x] **Manual Provider** — copy-paste 兜底, priority 1
- [x] **`ai-config set-provider` 子命令** — 切换默认 Provider

### Milestone 6: Generators & Checks ✅ DONE (2026-02-10)

> 实现设计文档中规划的自动化工具链。

#### Generators
- [x] `GitHooksGenerator` — 生成 pre-commit / pre-push（自动检测 Cargo/npm/Python）
- [x] `CiCdGenerator` — GitHub Actions（Rust/Node.js/Generic 模板）
- [x] `MakefileGenerator` — 标准 make targets
- [x] `IdeGenerator` — VS Code settings/extensions

#### Checks
- [x] `SensitiveInfoCheck` — 正则扫描 API key/密码/证书
- [x] `TodoCheck` — TODO/FIXME/HACK/XXX 检测
- [x] `LintCheck` — 调用 cargo clippy / eslint / ruff
- [x] `TestCheck` — 调用 cargo test / npm test / pytest

### Milestone 7: Pass Engine 核心（预计 4-6 天）⭐⭐ 架构转型

> 从散装命令重构为 Pass Engine。这是实现 DESIGN.md 愿景的关键里程碑。

#### 7a. Pass Trait 与 Artifact 模型
- [ ] **定义 `Pass` trait** — name, requires, produces, kind, interaction, run
- [ ] **定义 `ArtifactKind` enum** — Seed, Proposals, Rules, Skeleton, DecisionLog, Custom(String) 等
- [ ] **`PassContext`** — 读写 artifact、访问配置、调用 AI 的统一上下文

#### 7b. Manifest 系统
- [ ] **`manifest.yaml` 结构** — artifact 登记簿（来源、版本、hash、依赖）
- [ ] **Manifest 读写** — 每个 pass 执行后自动更新 manifest
- [ ] **新鲜度检查** — 判断 artifact 是否需要重新生成（类似 Make）

#### 7c. PassManager（调度器）
- [ ] **Pass 注册** — 内置 pass 的静态注册
- [ ] **依赖解析** — 拓扑排序，构建 DAG
- [ ] **执行引擎** — 按序执行 pass，支持并行执行无依赖的 pass
- [ ] **错误处理** — pass 失败时停止，报告哪个 pass 在哪个 artifact 上失败

#### 7d. 迁移现有命令
- [ ] **将现有 9 个命令重构为 Pass** — init→seed.init, diverge→diverge.generate 等
- [ ] **Phase 作为 preset** — `process diverge` 展开为 `diverge.*` 的所有 pass
- [ ] **`process pass run <name>`** — 手动运行单个 pass 的命令
- [ ] **`process pass list`** — 列出所有可用 pass

### Milestone 8: 打磨 & 发布（预计 2-3 天）

- [ ] **Review Templates** — 实现 `process-reviews` crate（general/security/performance/architecture）
- [ ] **帮助系统** — `process help`，按类别分组显示命令
- [ ] **Shell 补全** — clap 自动生成 bash/zsh/fish 补全
- [ ] **错误信息美化** — 使用 `miette` 或自定义错误格式
- [ ] **README 完善** — 安装说明、Quick Start、截图
- [ ] **发布** — `cargo install` / Homebrew / 预编译 Binary

---

### Milestone 9: Pass 插件生态（预计 5-7 天）⭐ 生态化

> 从"一个工具"变成"一个平台"。让社区可以贡献 Pass 和决策知识。

#### 9a. 外部 Pass 发现与加载
- [ ] **Pass Manifest 规范** — 每个外部 pass 是一个目录 + `pass.yaml` 声明
- [ ] **Pass 发现** — 扫描全局 `~/.config/process-cli/passes/` + 项目 `.process/passes/`
- [ ] **统一注册** — 内置 pass 和外部 pass 在 PassManager 中统一管理

#### 9b. 脚本桥接层（Script Bridge）
- [ ] **JSON-RPC 协议** — 外部 pass 通过 stdin/stdout JSON-RPC 通信
- [ ] **任意语言支持** — Python/JS/Shell 都能写 pass
- [ ] **错误隔离** — 外部 pass 崩溃不影响主进程

#### 9c. 知识库插件（纯数据，最低门槛）
- [ ] **challenge-library 类型** — 社区贡献的决策陷阱提醒库
- [ ] **领域匹配** — 根据 seed.yaml 自动匹配适用的 challenge
- [ ] **review-template 类型** — 领域特定的审查视角

#### 9d. 插件管理命令
```bash
process plugin list
process plugin install <path-or-git-url>
process plugin create <name>
```

#### 9e. 领域包（Domain Pack）
- [ ] **领域包结构** — passes + pipelines + prompts + schemas
- [ ] **`process init --pack <name>`** — 用指定领域包初始化
- [ ] **内置 software pack** — 将现有软件开发流程打包为第一个领域包

### Milestone 10: 决策周期管理（预计 3-5 天）⭐ 生命周期

> 让 Process CLI 能陪伴项目的整个生命周期，支持多轮决策周期。

#### 10a. 周期管理
- [ ] **`process cycle new <name>`** — 在现有项目上开启新的决策周期
- [ ] **`process cycle list`** — 列出所有历史周期
- [ ] **`process cycle close`** — 关闭当前周期（触发 postmortem）
- [ ] **周期间 artifact 隔离** — 每个周期有独立的 artifact 空间，共享 knowledge

#### 10b. Manifest 时间线
- [ ] **manifest.yaml 中的 cycles 字段** — 记录周期 id、类型、时间、触发原因、父周期
- [ ] **周期类型** — greenfield / evolution / maintenance / sunset

#### 10c. 生命周期 Watch Pass
- [ ] **`watch.rule-drift`** — 检测代码是否偏离 rules.yaml
- [ ] **`watch.decision-trigger`** — 检查 revisit_when 条件是否触发
- [ ] **`watch.tech-debt`** — 追踪技术债积累

### Milestone 11: 知识传递系统（预计 3-5 天）⭐ 知识积累

> 让决策经验跨周期、跨项目积累，形成"决策基因组"。

#### 11a. 知识库结构
- [ ] **项目级知识** — `.process/knowledge/`（heuristics.yaml, lessons.yaml）
- [ ] **个人级知识** — `~/.config/process-cli/knowledge/`（跨项目积累）
- [ ] **知识衰减** — 每条知识带 preconditions 和 revisit_when

#### 11b. 知识传递机制
- [ ] **显式约束传递** — 上一周期的 rules 自动成为下一周期的前置约束
- [ ] **上下文注入** — 历史决策作为参考注入到 pass 的 prompt 中
- [ ] **启发式提炼** — AI 辅助从多个周期的教训中提炼模式

#### 11c. 决策质量画像
- [ ] **`process report decision-profile`** — 生成决策者画像（HTML）
- [ ] **四维度评估** — 考虑充分性、推理显式性、预测校准度、学习闭环性
- [ ] **跨周期趋势** — 追踪决策质量随时间的变化

### Milestone 12: 决策语言（预计 2-3 天）

> 让 rules.yaml 从纯自然语言进化为半结构化的可执行约束。

- [ ] **可检测规则** — 附带 grep pattern 或 command 的自动检测
- [ ] **不可检测规则** — 只在 review pass 中作为提示
- [ ] **severity 分级** — hard（必须遵守）/ soft（建议）
- [ ] **scope 限定** — 规则适用的文件范围（glob pattern）
- [ ] **与 watch pass 集成** — watch.rule-drift 读取检测规则自动执行

---

## 四、优先级总览

```
阶段一：功能完备（让工具可用）
┌───────────┐   ┌───────────┐   ┌──────────────┐  ┌──────────────┐
│ MS0: 修复  │   │ MS1: Bash │   │ MS1.5: 决策  │  │ MS2: Adopt   │
│ 基础 bug   │──▶│ 功能平替   │──▶│ 深度增强 ⭐⭐ │──▶│ Pass 组 ⭐   │
└───────────┘   └───────────┘   └──────────────┘  └──────────────┘

阶段二：能力增强（让工具好用）
┌───────────┐   ┌───────────┐   ┌───────────┐
│ MS3: Prompt│   │ MS4: 测试  │   │ MS5: AI   │
│ 系统       │   │ 体系       │   │ Provider  │    可并行推进
│ MS6: 工具  │   │ MS12: 决策 │   │           │
│ 链         │   │ 语言       │   │           │
└───────────┘   └───────────┘   └───────────┘

阶段三：架构转型（让工具可扩展）
┌──────────────┐   ┌───────────┐
│ MS7: Pass    │   │ MS8: 打磨  │
│ Engine ⭐⭐   │──▶│ & 发布     │
└──────────────┘   └───────────┘

阶段四：生态与生命周期（让工具有生命力）
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│ MS9: Pass    │   │ MS10: 决策   │   │ MS11: 知识   │
│ 插件生态 ⭐  │   │ 周期管理 ⭐  │   │ 传递系统 ⭐  │
└──────────────┘   └──────────────┘   └──────────────┘
```

**四条路径：**

- **功能路径**: MS0 → MS1 → MS1.5 → MS2（让工具体现"决策训练"的定位）
- **能力路径**: MS3 + MS4 + MS5 + MS6 + MS12（并行推进）
- **架构路径**: MS7 → MS8（Pass Engine 转型 + 发布）
- **生态路径**: MS9 + MS10 + MS11（插件生态 + 生命周期 + 知识积累）

---

## 五、设计原则（从 brainstorm 中提炼）

> 详细的架构愿景见 [DESIGN.md](DESIGN.md)

### 原则 1: 一切皆 Pass

所有功能都是 Pass。Phase 是 Pass 的预设组合（Auto 模式）。用户可以单独运行任何 Pass。

### 原则 2: Artifact 驱动

Pass 之间不直接引用彼此，通过 artifact 间接耦合。Manifest 追踪所有 artifact 的来源和依赖。

### 原则 3: 决策是一等公民

每个决策有生命周期（active → superseded → archived）、演化链、可扩展的元数据。

### 原则 4: 知识会积累

决策经验跨周期、跨项目积累。知识带保质期和前提条件，过时的知识会被标记。

### 原则 5: 引擎不可插拔，引擎上的一切可插拔

最小内核只有五样：Artifact 管理、Pass 调度器、Pass 注册、Pipeline 引擎、CLI 框架。

### 原则 6: 反官僚主义不是工具的责任

工具提供结构，不强制态度。用户自己对决策质量负责。

---

## 六、完成度

| 里程碑 | 状态 | 完成度 | 意义 |
|--------|------|--------|------|
| MS0: 基础修复 | ✅ 完成 | 100% | 地基 |
| MS1: Bash 功能平替 | ✅ 完成 | 100% | 完整流程 |
| **MS1.5: 决策深度增强** | ✅ 完成 | 100% | **⭐⭐ 核心差异化** |
| MS2: Adopt Pass 组 | ✅ 完成 | 100% | 已有项目支持 |
| MS3: Prompt 系统 | 🔴 未开始 | 0% | 可定制、per-provider |
| MS4: 测试体系 | 🟡 微量 | 5% | 质量保障 |
| MS5: 更多 AI Provider | 🟡 部分 | 20% | 多模型支持 |
| MS6: Generators & Checks | 🔴 未开始 | 0% | 自动化验证 |
| **MS7: Pass Engine** | 🔴 未开始 | 0% | **⭐⭐ 架构转型** |
| MS8: 打磨 & 发布 | 🔴 未开始 | 0% | 发布 |
| **MS9: Pass 插件生态** | 🔴 未开始 | 0% | **⭐ 社区生态** |
| **MS10: 决策周期管理** | 🔴 未开始 | 0% | **⭐ 全生命周期** |
| **MS11: 知识传递系统** | 🔴 未开始 | 0% | **⭐ 决策基因组** |
| MS12: 决策语言 | 🔴 未开始 | 0% | 半结构化规则 |

**总体进度: ~40%**（MS0-MS2 已完成，全部命令已实现，决策深度增强已就位，Adopt Pass 组已实现。下一步：MS3 Prompt 系统）
